import { AlldircDirective } from './alldirc.directive';

describe('AlldircDirective', () => {
  it('should create an instance', () => {
    const directive = new AlldircDirective();
    expect(directive).toBeTruthy();
  });
});
